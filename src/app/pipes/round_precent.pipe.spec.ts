import { RoundPercentPipe } from './round_percent.pipe';

describe('RoundPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
