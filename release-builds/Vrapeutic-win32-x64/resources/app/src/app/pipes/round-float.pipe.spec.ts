import { RoundFloatPipe } from './round-float.pipe';

describe('RoundFloatPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundFloatPipe();
    expect(pipe).toBeTruthy();
  });
});
